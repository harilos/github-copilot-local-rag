# AGENTS.md

## 適用範囲

この指示は、このファイルが置かれたリポジトリとその配下の作業に適用する。

## ユーザーによる包括的な事前許可

ユーザーは、依頼されたタスクを完了するために必要な次の操作を、都度の確認なしで実行することを明確に許可している。

- リポジトリ内のファイル、Git履歴、設定、ログ、テスト結果の読み取りと検索
- `git status`、`git diff`、`git log`、`git show`、`git branch`、`git fetch`等の非破壊的な調査
- 依存関係、ソースコード、テスト、ビルド設定の確認
- テスト、lint、型チェック、ビルド、再現試験、静的解析の実行
- タスク専用のbranch／clone／worktreeの作成と利用
- タスク達成に必要なファイルの作成・編集
- タスク専用branchへのcommitと通常push
- 専用fixture、一時ファイル、テスト用DB、ビルド生成物の作成
- 失敗原因の調査と、依頼範囲内の修正・再検証

上記について、単なる読み取り、コマンド実行、ファイル編集、commit、専用branchへのpushを理由にユーザーへ承認を求めない。細かな操作単位で承認を分割しない。依頼の完了条件まで自律的に進める。

読み取りや非破壊的確認を、承認待ち・保留・確認質問へ変換しない。安全な通常操作が実行環境の制約で失敗した場合は、同じ目的を達成できる許可済みの安全な方法を試す。

## 必ず停止して確認する操作

次の操作は包括許可に含まれない。実行前に、対象・影響・復旧方法を示してユーザーの明示確認を得る。

- `main`、`master`その他の保護ブランチへのmergeまたは直接push
- PRのmerge、tag作成、Release作成、公開配布物の更新
- force push、履歴改変、branch削除
- `reset --hard`、`clean`、広範囲の削除・上書き等、既存データを失う可能性がある操作
- ユーザーの既存未コミット変更を移動・破棄・書換えする操作
- GitHub Issue／PRコメント、メール、Slack、Discord等への外部送信
- 本番環境、実利用者環境、実データ、実DB、クラウド資源への変更
- credential、token、秘密鍵、個人情報、機密情報の表示・移動・保存・送信
- 当初の依頼範囲を実質的に拡張する作業

ただし、ユーザーが個別タスクで上記操作まで明示的に依頼した場合は、その明示範囲を当該タスクの許可として扱う。同じ操作について重ねて承認を求めない。

## Git作業の基準

- 編集を伴う作業では、指定されたbranchを使う。指定がなければ内容が分かるタスク専用branchを作る。
- 専用branchへのcommit・通常pushまでは事前許可済みである。
- `main`への反映は、ユーザーがそのタスクで明示的に許可していない限り行わない。
- 作業開始時に未コミット変更を確認し、ユーザー所有の変更を保持する。無関係な変更をcommitへ混ぜない。
- 破壊的なGit操作を、作業ツリーをきれいにする目的で使わない。

## 承認判断の原則

承認の要否は、コマンド名ではなく実際の影響で判断する。

- 読み取り・検証・専用branch内の可逆的変更は、原則として承認不要。
- 保護ブランチ、公開、外部送信、秘密情報、実環境、不可逆変更に境界を越える場合のみ停止する。
- 「念のため」「安全側だから」だけを理由に、許可済み操作の承認を増やさない。
- 一つの目的を達成する連続した許可済み操作は、一連の作業として扱う。
- ツールや実行基盤が強制する権限確認は回避しない。ただし、それをユーザー判断が必要な製品上の承認と混同しない。

## 報告

途中経過は簡潔にまとめ、個々の読み取りコマンドを逐一報告しない。完了時は、変更内容、検証結果、branch、commit SHA、push状況、残るリスクだけを報告する。
