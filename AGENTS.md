# AGENTS.md

## 適用範囲

この指示は、このファイルが置かれたリポジトリとその配下の作業に適用する。

本リポジトリは未告知で、実質的な単独開発段階にある。正式なチーム開発手続きを機械的に適用せず、戻しにくい事故だけを強く防ぐ。

## ユーザーによる包括的な事前許可

ユーザーは、依頼されたタスクを完了するために必要な次の操作を、都度の確認なしで実行することを明確に許可している。

- リポジトリ内のファイル、Git履歴、設定、ログ、テスト結果の読み取りと検索
- `git status`、`git diff`、`git log`、`git show`、`git branch`、`git fetch`等の非破壊的な調査
- 依存関係、ソースコード、テスト、ビルド設定の確認
- タスク達成に必要なファイルの作成・編集
- 関連テスト、lint、型チェック、ビルド、再現試験、静的解析の実行
- 必要に応じたbranch／clone／worktreeの作成と利用
- scope内の修正、commit、通常push
- 関連テスト合格後の`main`への通常mergeまたは直接commit、および通常push
- 専用fixture、一時ファイル、テスト用DB、ビルド生成物の作成
- 失敗原因の調査と、依頼範囲内の修正・再検証

上記について、単なる読み取り、コマンド実行、ファイル編集、commit、branch作成、`main`への通常merge／pushを理由にユーザーへ承認を求めない。細かな操作単位で承認を分割せず、依頼の完了条件まで自律的に進める。

読み取りや非破壊的確認を、承認待ち・保留・確認質問へ変換しない。安全な通常操作が実行環境の制約で失敗した場合は、同じ目的を達成できる許可済みの安全な方法を試す。

## 必ず停止して確認する操作

次の操作は包括許可に含まれない。実行前に、対象・影響・復旧方法を示してユーザーの明示確認を得る。

- tag作成、GitHub Release作成、配布ZIP等の公開、既存公開物の差替え
- force push、履歴改変、remote branch削除
- `reset --hard`、`clean`、広範囲の削除・上書き等、既存データを失う可能性がある操作
- ユーザーの既存未コミット変更を移動・破棄・書換えする操作
- GitHub Issue／PRコメント、メール、Slack、Discord等への外部送信
- 本番環境、実利用者環境、実データ、実DB、クラウド資源への重大または破壊的な変更
- credential、token、秘密鍵、個人情報、機密情報の表示・移動・保存・送信
- 当初の依頼範囲を実質的に拡張する作業

ただし、ユーザーが個別タスクで上記操作まで明示的に依頼した場合は、その明示範囲を当該タスクの許可として扱う。同じ操作について重ねて承認を求めない。ツールや実行基盤が強制する権限確認は回避しない。

## Git作業の基準

- 小規模な通常修正は`main`へ直接commitしてよい。
- 少し大きな実装や並行作業は、内容が分かる短期branchを使い、関連テスト後に`main`へ通常merge／pushする。
- branch、PR、独立レビュー、全件試験は、明確な品質上の効果がある場合だけ使う。正統的な手順という理由だけで追加しない。
- 全件試験を既定にせず、変更範囲に対応する関連テストを優先する。
- commitは、問題発生時に追跡・fix-forwardしやすい意味のある単位にする。
- 作業開始時に未コミット変更を確認し、ユーザー所有の変更を保持する。無関係な変更をcommitへ混ぜない。
- force pushや破壊的なGit操作を、作業ツリーをきれいにする目的で使わない。

## GitHub CLI認証（Windows Codex）

- Windows Codexは通常ユーザーと異なる資格情報領域で動く場合がある。ブラウザに認証成功と表示されたことや、公開repositoryへの`fetch`成功だけを、write認証成功の根拠にしない。
- ファイルサンドボックス内の`gh auth login`は、device認証の完了後に`AppData\Roaming\GitHub CLI\hosts.yml: Access is denied`となり、認証を保存できない場合がある。この表示を成功扱いせず、ACL変更や`--insecure-storage`で迂回しない。対話認証が必要な場合は、明示承認されたサンドボックス外実行で`gh auth login --hostname github.com --git-protocol https --web`を開始する。
- 認証3ゲートと認証を使うwrite／`push`は、認証を保存・読取できる同じ権限境界で実行する。通常サンドボックスでの`401`と、サンドボックス外での成功を混ぜて判定しない。
- GitHubへのwriteや`push`の直前に、同じ実行環境で次の3ゲートを順に確認する。
  1. `gh auth status --active --hostname github.com`が終了コード0。
  2. `gh api user --jq .login`が終了コード0で、期待するaccount（このrepositoryでは`harilos`）を返す。
  3. `gh api repos/harilos/github-copilot-local-rag --jq .permissions.push`が終了コード0で、出力が厳密に`true`。
- 終了コード0でも空出力は合格にしない。pipeや複合コマンドで直前の失敗を隠さず、各ゲートの終了コードと値を個別に検査する。
- `401`は認証失敗、`403`はscope、repository access、SSO、ruleset等の認可問題として区別する。いずれかのゲートが不合格ならGitHub writeを行わず、`AUTH_REQUIRED`として停止する。
- one-time device codeは認証中にユーザーへ一度提示する以外、repository、設定、永続ログへ保存しない。tokenや秘密値はprompt、コマンド引数を含むいずれの出力にも載せない。`gh auth status --show-token`、出力を保持する`gh auth token`、`GH_DEBUG=api`、`--insecure-storage`を使わない。
- 認証待ちの間に対象が変わっていないことを`git status`とHEADで再確認してからpushし、push後はremote branchのSHAを再取得して期待HEADと一致することを確認する。

## 品質と承認判断の原則

- レビュー内容は重めに、運用手続きは軽めにする。
- 検索品質、秘密情報、DB整合性、インストーラー、配布経路など高危険度の差分は重点的に確認する。
- 一時的に`main`へ不具合が入るリスクは許容し、発見時はrevertを前提にせず小さな後続commitでfix-forwardする。
- 追加工程を入れる場合は、何の事故をどの程度防ぐためかを説明できること。
- 「念のため」「安全側だから」だけを理由に、許可済み操作の承認を増やさない。
- 一つの目的を達成する連続した許可済み操作は、一連の作業として扱う。

## 報告

途中経過は簡潔にまとめ、個々の読み取りコマンドを逐一報告しない。完了時は、変更内容、検証結果、branch、commit SHA、push状況、残るリスクだけを報告する。
